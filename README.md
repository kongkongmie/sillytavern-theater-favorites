# 小剧场收藏夹

SillyTavern 第三方扩展，用来收藏、阅读和管理聊天中的小剧场。

这是一个独立插件，不要求安装 KKM Tools。插件由“前端扩展”和“服务插件”两部分组成，两部分都安装后才能把收藏完整保存到本地文件。

## 安装：只需要三步

### 第一步：在酒馆里安装

打开 SillyTavern 的“扩展”面板，点击“安装扩展”，粘贴：

```text
https://github.com/kongkongmie/sillytavern-theater-favorites
```

### 第二步：运行一次后端安装器

Windows / PC：双击下面这个文件：

```text
SillyTavern/public/scripts/extensions/third-party/theater-favorites/install-server-plugin.bat
```

安卓 Termux / Linux / Mac / 云服务器：运行：

```sh
node SillyTavern/public/scripts/extensions/third-party/theater-favorites/install-server-plugin.js
```

安装器会自动复制后端、备份配置并开启后端插件功能，不需要手动修改文件。

后端与配置备份统一保存在：

```text
SillyTavern/backups/theater-favorites/
```

### 第三步：重启酒馆

看到“安装完成”后，重启 SillyTavern，再刷新浏览器。打开小剧场收藏夹的“设置”，看到“前端已加载”和“后端已连接”就成功了。

## 功能

- 默认识别 `<snow>...</snow>`，可添加其他标签。
- 支持识别 `details` 折叠结构。
- 每个小剧场旁显示收藏按钮。
- 管理面板一次只渲染当前展开的小剧场。
- HTML 小剧场在隔离预览中运行。
- 使用完整内容 SHA-256 防止重复收藏。
- 支持搜索、角色/聊天/来源筛选、自定义标签、重命名、编辑正文和手动排序。
- 支持完整 JSON 备份的导入导出，以及浏览器可打开的 HTML 阅读副本。
- 实验性兼容拟界文库，可在识别设置中关闭。

## v0.4.5 CSS 显示正则热修

- 修复依赖 SillyTavern 页面 CSS class 的显示正则在收藏预览中丢失样式的问题。
- 自带完整样式或脚本的小剧场继续在隔离预览中运行；只依赖酒馆样式的结构会安全继承页面 CSS。
- 收藏夹固定文字颜色不再覆盖 CSS 小剧场自己的配色。
- 从 v0.4.4 更新后刷新页面即可，不需要重新安装后端。

## v0.4.4 性能热修

- 修复收藏夹观察到自己添加的按钮和图标后，再次触发整页扫描的自循环问题；酒馆空闲时不再持续遍历聊天记录。
- 新消息和流式输出只处理真正发生变化的消息，不再因为菜单、图片或其他扩展的普通 DOM 变化重新扫描全部历史消息。
- 点击收藏时会重新读取对应消息，保存最新内容；无变化的按钮不会重复重建 HTML 或绑定事件。
- 智绘姬图片复用一次建立索引，不再为每个图片提示词重复搜索所有 iframe 和按钮。
- 拟界文库取消固定轮询，HTML 预览高度同步忽略无关的 style/class 动画变化。
- 新增“更新”页面：每次页面会话首次打开收藏夹时只检查一次版本号；完整更新内容仅在用户打开更新页或点击检查时读取，发现新版本后由用户决定是否安装，不会后台轮询或自动更新。
- 面板文字使用固定的深色界面配色，避免日间主题或美化覆盖按钮文字；入口统一使用 SillyTavern 自带的 Font Awesome 剧场图标，不再依赖图片资源。
- 修复超过 20 条收藏时翻页栏不可见；修复显示正则与 Markdown 没有应用到收藏预览；限制异常 HTML 预览高度，超长内容改在预览内部滚动。
- 从 v0.4.3 更新不需要重新运行后端安装器；在扩展面板更新后完整刷新一次页面即可。

### 维护更新内容

更新页面显示的文字来自仓库根目录的 `updates.json`。每次发布前由维护者手动修改 `latest`，并在 `releases` 最前面填写版本、日期、标题和 `items`；插件不会从提交记录自动生成文案。`manifest.json` 的版本必须与 `updates.json` 的 `latest` 保持一致。

## v0.4.3 手机端与入口修复

- 修复部分手机浏览器打开收藏夹后，面板被压成页面顶部一条黑线的问题。
- 修复电脑端、手机端入口图标受主题或 CSS 遮罩兼容性影响而不可见的问题；原图加载失败时会自动显示备用图标。
- 修复 `<snow>` 中嵌套 `<details>` 时折叠标题和正文不渲染的问题，并隐藏正文中的图片生成提示词块。
- 具有 HTTP(S) 或酒馆相对地址的已渲染图片只保存地址引用，不下载图片文件；纯 Base64、临时 Blob 或没有真实地址的生成图不会保存。图片原地址失效后，收藏中的图片也会失效。
- 可选兼容智绘姬 v2.7.8：无地址图片提示词可由用户手动调用智绘姬生成。按钮会明确提示可能消耗额度；生成结果只存在于当次预览，不会写入收藏。
- 当前聊天里已经存在对应智绘姬生成图时，会优先在收藏预览中临时复用该图，即使它是 Base64；图片数据仍不会写入收藏，离开当前聊天后可能无法再次显示。
- 从 v0.4.2 更新不需要重新运行后端安装器，也不会影响已有收藏；在酒馆扩展面板更新后刷新页面即可。

## v0.4.2 安装器热修

- 修复旧后端备份被放在 `plugins` 目录后，酒馆把它们误当成多个服务器插件并尝试 Git 更新的问题。
- 重新运行安装器会自动把旧备份迁移到 `SillyTavern/backups/theater-favorites/`，今后的后端和配置备份也统一保存在这里。
- 复制式安装的收藏夹后端会阻止 Git 向上误认 SillyTavern 根仓库，不再由收藏夹触发 `detected dubious ownership`。
- 安装失败时批处理不会再错误显示 `Done`；安装器会尽量恢复更新前的旧后端。
- v0.4.2 不修改其他服务器插件，也不会自动写入全局 Git `safe.directory`。

### 如果启动时仍显示 `detected dubious ownership`

先确认报错里的插件名称。更新到 v0.4.2 并重新运行安装器后，`theater-favorites` 和 `theater-favorites.backup-*` 不应再触发该错误。

如果剩下的是 tavern-notes 或其他服务器插件，说明 SillyTavern 根目录的所有者与当前运行账号不同，这是该插件或酒馆安装目录自身的 Git 权限问题。可以在 `config.yaml` 中设置：

```yaml
enableServerPluginsAutoUpdate: false
```

然后重启酒馆，服务器插件改为手动更新。只有在确认该 SillyTavern 目录确实属于自己且可信时，才应考虑执行 Git 错误信息给出的 `safe.directory` 命令。

## v0.4.1 修复

- 修复 Windows 用户双击 `install-server-plugin.bat` 时，路径里有空格会被 `cmd.exe` 拆坏，导致出现“不是内部或外部命令”的问题。
- 修复后端安装器提示乱码；批处理入口改为纯英文提示，Node 安装器继续输出中文安装结果。
- 新增 Markdown 渲染快照识别：标签型小剧场原文像 Markdown、且酒馆消息里已经渲染出粗体、斜体、引用、代码、链接或列表时，收藏夹会保存酒馆渲染后的安全快照用于预览；拿不到快照时仍按纯文本保底。
- 修复展开长收藏后删除条目时，弹窗外壳被浏览器自动滚动、顶部标题和操作按钮消失的问题。
- Markdown 预览会自动隐藏 `<!-- ... -->` 注释内容，同时保留收藏原文；新增全局标签管理，可一次性从所有收藏中移除指定标签。
- 手机端改为紧凑标题和工具栏；搜索始终显示，其他筛选收进“筛选”按钮，单条编辑操作收进“管理这条收藏”，并扩大 HTML 预览的可用宽度和最小高度。
- 不需要迁移已有收藏；由于标签管理新增了后端接口，更新到 v0.4.1 后请重新运行一次后端安装器并重启 SillyTavern。若 v0.4.0 安装器曾报错，也会一并修复。

## v0.4.0 新功能

- 展开收藏后可以点击“编辑正文”，直接修改这条小剧场的原文；保存后会同步更新渲染预览、搜索内容、导出内容和去重签名。
- 列表左侧不再显示序号，改为拖拽把手；拖动收藏即可调整顺序，刷新页面后顺序会保留。“上移 / 下移”按钮保留为备用操作。
- 旧收藏无需迁移，没有手动顺序的旧数据会继续按原收藏时间显示。
- 快捷入口会自动隐藏旧入口残留，避免快捷回复栏出现两个收藏夹图标或重复入口反复闪烁；手机扩展菜单里也不再显示破图图标。
- 纯文字小剧场里即使嵌套其他识别标签，也会按纯文本保存和预览，保留换行，不再被浏览器当 HTML 吞掉格式。

## v0.3.1 修复

- 固定深色收藏夹不再读取日间主题的黑色文字变量，筛选框、列表和操作按钮在日间美化下仍保持清晰。
- 渲染预览会提供深色画布的默认文字与控件配色，同时保留小剧场作者明确设置的背景和文字颜色。
- 发送栏快捷入口改为独立样式，避免美化主题将入口文字放大成遮挡输入栏的悬浮框；面板关闭态会强制隐藏，也可按 `Esc` 关闭。
- 纯文字小剧场的渲染预览直接使用原始正文并保留换行，不再挤成一整段；已有收藏无需重新保存。
- 手机端入口固定在快捷回复区第一位，并会在酒馆重建快捷回复栏后自动恢复，不再消失或挤进输入区。
- 快捷入口继续使用收藏夹自己的原始面具图形，但按钮配色、圆角和图标颜色会跟随酒馆当前主题；只独立锁定安全尺寸与定位，因此能与同排图标保持一致且不再被主题拉伸。部分深色主题下也会避开原生按钮底色，继续使用酒馆快捷回复按钮风格。

## 本地存储

收藏内容完整保存在 SillyTavern 当前用户目录，不截断正文、HTML 或脚本。

```text
SillyTavern/data/<当前用户>/theater-favorites/
|-- index.json
`-- items/
    |-- tf_xxx_000001.json
    `-- tf_xxx_000002.json
```

`index.json` 只保存标题、日期、SHA-256、大小等轻量目录信息。每个小剧场使用独立 JSON 文件，点开时才读取对应文件，单条删除会直接释放该文件。索引使用临时文件替换并保留备份；索引损坏时可以从 `items` 重建。

超过 20 MB 的单条收藏会在加载预览前提示。设置中的“本地存储”可以查看占用、整理孤立文件或清空全部收藏。

旧版 JSONL 数据会在服务插件启动后自动迁移，成功后才移除旧分片。

完整备份 JSON 用于迁移和恢复。HTML 导出仅用于浏览器阅读，不能代替完整备份。

## 与 KKM Tools 的关系

KKM Tools 只提供可选入口。没有安装 KKM Tools 时，小剧场收藏夹仍会显示自己的工具栏入口并可独立使用；没有安装小剧场收藏夹时，KKM Tools 的其他功能也不受影响。

## 隐私与权限

- 收藏内容只发送到当前 SillyTavern 实例的本地服务插件。
- 服务插件只读写当前用户目录下的 `theater-favorites` 文件夹。
- 插件自身不上传收藏内容。
- HTML 小剧场可能包含作者编写的脚本或外部资源；互动预览与 HTML 导出应视为运行该小剧场代码。

## 许可证

[MIT License](LICENSE)
